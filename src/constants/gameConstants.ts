export const BIRD_HEIGHT = 32;
export const BIRD_WIDTH = 32;
export const PIPE_WIDTH = 60;
export const PIPE_GAP = 180; // Increased gap between pipes
export const GRAVITY = 0.4; // Reduced gravity
export const JUMP_FORCE = -7; // Reduced jump force for more control
export const PIPE_SPEED = 2.5; // Slightly slower pipe movement
export const BIRD_X = 50;

export const PIPE_CONFIG = {
  MIN_HEIGHT: 50,
  MAX_HEIGHT: 250, // Reduced maximum pipe height
  SPAWN_X: 400,
  SPAWN_DISTANCE: 220, // Increased distance between pipes
};